node_modules/
dist/
build/
release/
*.tsbuildinfo
.DS_Store
.env
.env.local
*.log
.vite/

# Mock-provider demo artifact
MOCK_DEMO.md

# Crash reports from error-handler skill — users upload .md manually to GitHub, not via git
.windows-runner/crash-reports/
crash-reports/

# Playwright output (packages/web)
test-results/
playwright-report/

# Eval reports: the committed sample lives in eval/results/ (see
# eval/results/scripted-2026-09-20.json). Locally generated runs are ignored
# so they can never be staged into a commit by accident; committing a new
# report is a deliberate `git add -f`.
eval-scripted.json
eval/results/scripted-*.json
eval/results/validate-*fake*.json
