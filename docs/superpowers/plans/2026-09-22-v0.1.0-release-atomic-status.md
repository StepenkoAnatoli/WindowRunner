# v0.1.0 release — atomic status

Durable record of the first-tagged-release session (2026-09-22): verify the
post-B5 main, record the B5 merge, cut `v0.1.0`, and hand off the owner
actions. Chat history is not durable — this file is.

Branch: `arena/01a0c994-windowrunner`
Base at session start: `main` @ `65d5317` (merge of PR #30, B3's record)
Main at session end: `e06c191` (merge of PR #34)
Session brief: the post-B5 handoff plan (steps 1–6) supplied at session
start; this file records its execution.

## Status

**All session steps complete through a verified DRAFT release.** Step 1
(post-merge main verified), Step 2 (B5 merge recorded, PR #33) and Step 3
(`v0.1.0` cut, after one diagnosed-and-fixed release-guard failure) are
done. Publishing is deliberately **not** done — the release workflow's own
contract is draft-only and the unsigned-vs-wait-for-certificate decision
belongs to the maintainer (see Known gaps). Steps 4 (production signing
certificate) and 5 (branch protection) remain owner/admin actions; both were
re-verified as not performable by this token.

## What happened, in order

1. **Post-merge main verified (Step 1).** Run
   [35736971857](https://github.com/StepenkoAnatoli/WindowRunner/actions/runs/35736971857)
   on the B5 merge `84aacd3`: 9/9 green. Additionally, main had moved once
   more before this session: PR #30 (B3's merge record, docs-only) merged
   after B5 as `65d5317`, and its post-merge run
   [35742013439](https://github.com/StepenkoAnatoli/WindowRunner/actions/runs/35742013439)
   is 9/9 green as well — the tag base was verified on the actual tip.
2. **B5 merge recorded (Step 2, PR #33 → `ae8fac2`).** Closing note, Status,
   CI-status and Verdict sections of `2026-09-22-b5-atomic-status.md`
   updated with the merge commit and both green runs, following the B3
   pattern (PR #30). Companion change: CHANGELOG.md's `[0.1.0]` section said
   "(pending the first tagged release …)" — that text ships verbatim as the
   release notes (`scripts/release-notes.mjs`), so it was reworded to a
   timeless sentence; `[Unreleased]` wording made timeless too. PR run
   [35744980421](https://github.com/StepenkoAnatoli/WindowRunner/actions/runs/35744980421)
   9/9; post-merge run
   [35745916114](https://github.com/StepenkoAnatoli/WindowRunner/actions/runs/35745916114)
   9/9.
3. **`v0.1.0` cut (Step 3) — first attempt failed, root-caused, fixed.**
   - Tag pushed on `ae8fac2` (nine checks green, tag-bound guard verified
     locally first: `check:release -- --require-version v0.1.0` OK).
   - Release run
     [35746819899](https://github.com/StepenkoAnatoli/WindowRunner/actions/runs/35746819899)
     **failed in the guard**: `npm test` on the fresh checkout failed at
     `packages/server/test/packaging.test.ts` → "clean-install artifact
     execution" — `packages/server/dist/index.cjs must exist (run build
     first)` (384/385 server tests; the `&&` chain stopped before the web
     suite).
   - **Root cause:** `dist/` is gitignored and `postinstall` deliberately
     does not build; the unit suite's packaging contract executes the built
     bundle, so any job that runs `npm test` without building first fails on
     a fresh runner. The CI job has always built before `npm test`; the
     guard had never executed before the first tag (release.yml is
     contract-tested structurally; B5 deferred the first tag run to
     post-merge), so the ordering bug was latent until today.
   - **Why the local gate missed it (recorded so it cannot bite again):**
     `npm test` passed locally only because `npm run test:desktop` ran in
     parallel and its `server-process.test.ts` builds the server bundle as a
     side effect — a lucky race, not a real green. Reproduced
     deterministically in a fresh worktree at the tag: `npm test` without a
     build fails with exactly the CI error.
   - **Fix (PR #34 → `e06c191`):** the guard now runs `npm run build`
     between the tag-bound gate and the unit tests (the same order as the CI
     job), pinned by a new release-contract assertion ("builds before the
     guard's unit tests"). Proven end to end in a fresh worktree: `npm ci` →
     `check:release --require-version v0.1.0` → `npm run build` →
     `npm test` = 9/9 + 385/385 + 233/233, exit 0. Audited both workflows:
     every other testing job (CI, Platform matrix, Desktop matrix, cli,
     installer) already builds first; the guard was the only gap.
   - PR run
     [35749812077](https://github.com/StepenkoAnatoli/WindowRunner/actions/runs/35749812077)
     9/9; post-merge run
     [35750662456](https://github.com/StepenkoAnatoli/WindowRunner/actions/runs/35750662456)
     9/9.
   - **Tag moved** `ae8fac2` → `e06c191` (delete + re-create; the first push
     had produced no draft and no artifact, and nothing consumed the tag).
     Release run
     [35751619807](https://github.com/StepenkoAnatoli/WindowRunner/actions/runs/35751619807):
     **all four jobs green** — guard (gate → build → unit tests), CLI
     tarball (packed smokes), installer (unsigned mode — no cert secret;
     silently installed, e2e-driven, uninstalled), draft release created.
4. **Draft verified.** Exactly one draft exists (`WindowRunner v0.1.0`,
   draft=true, created 2026-09-22T15:55:57Z). Assets: the NSIS installer
   `WindowRunner-Setup-0.1.0.exe`, its `.blockmap` update metadata, the CLI
   tarball `windows-runner-0.1.0.tgz`, and `SHA256SUMS.txt` (three
   sha256sum-compatible lines, also embedded in the body). Notes: the
   changelog-derived `# WindowRunner v0.1.0 (2026-09-22)` section, the
   explicit **UNSIGNED warning banner** (WIN_CSC_LINK not configured;
   SmartScreen will warn; verify checksums or build from source), and the
   checksums block.

## Commits

```text
82154fe — docs: record the B5 merge and post-merge CI        (PR #33 → ae8fac2)
b2811da — fix(release): build before the guard's unit tests   (PR #34 → e06c191)
<this>   — docs(status): record the v0.1.0 release session
```

## Files changed

- `docs/superpowers/plans/2026-09-22-b5-atomic-status.md` — B5 merge record
  (closing note, status, CI table, verdict).
- `CHANGELOG.md` — retired the stale "pending the first tagged release"
  wording in `[0.1.0]` (it ships verbatim as the release notes) and the
  `[Unreleased]` accumulates-next-release wording.
- `.github/workflows/release.yml` — guard job gains a `Build` step
  (`npm run build`) before `Unit tests`, with a comment recording the
  incident.
- `packages/desktop/test/release-contract.test.ts` — new regression pin:
  the guard must run `run: npm run build` before `run: npm test`.
- `docs/superpowers/plans/2026-09-22-v0.1.0-release-atomic-status.md` —
  this file.

## Tests

| Check | Result |
| --- | --- |
| `npm run check:release` (+ `--require-version v0.1.0`) | pass (version 0.1.0, tag bound) |
| `npm run typecheck` / `typecheck:desktop` | pass (baseline, pristine tree) |
| `npm test` | pass — 627/627 locally; 385/385 in the fresh-worktree guard simulation |
| `npm run build` + `build:desktop` | pass |
| `npm run test:desktop` | pass — 69/69 (68 baseline + the new guard-order pin) |
| `npm run smoke:packed` / `smoke:packed:start` / `smoke:start` | pass |
| `npm run eval -- --expect-pass` | pass (5/5) |
| `npm audit --omit=dev --audit-level=high` | pass (0 vulnerabilities) |
| Fresh-worktree guard simulation (npm ci → gate → build → test) | pass — the exact fixed sequence |
| Electron/browser/Windows legs | executed by CI (sandbox limitation, same as B3/B4/B5) |

## Acceptance criteria (handoff steps)

- Step 1 post-merge main 9/9: **pass** (both `84aacd3` and `65d5317`).
- Step 2 B5 merge recorded, small docs PR, nine checks: **pass** (PR #33).
- Step 3 first tagged release cut: **pass** — draft exists with installer,
  tarball, checksums and changelog notes; publishing is the human step. The
  changelog date did not drift (2026-09-22).
- Step 4 production certificate: **not performable here** — maintainer
  action (buy OV/EV cert, add `WIN_CSC_LINK` + `WIN_CSC_KEY_PASSWORD`);
  zero code changes needed after that.
- Step 5 branch protection: **not performable here** — the automation token
  still gets HTTP 403 on branch-protection rules (re-verified this session);
  admin must require the nine checks + >= 1 approval on `main`.
- Non-goal respected — no provider/model-discovery code touched (B4).

## Security checks

- No secrets in the repo; signing stays env-driven; nothing about this
  session's diff touches secret handling.
- Release workflow permissions unchanged (`contents: read` default; the
  publishing job alone escalates to `contents: write`); the release is a
  draft, never published by automation.
- The draft's unsigned banner is present and names the SmartScreen
  consequence and the checksum verification path (finding F-01's mitigation,
  working as designed).
- Checksums travel both as a sidecar and in the body for human review.

## Deviations

- **The handoff's local-gate order is unsafe as written** ("npm test" before
  "npm run build"): on a fresh tree it fails (packaging contract), and with
  `test:desktop` running in parallel it produces a false green via the
  server-process build race. The safe order is typecheck → build → test
  (what CI does); recorded here for every future session.
- The `v0.1.0` tag was moved once (`ae8fac2` → `e06c191`) after the failed
  first release run. Nothing had consumed the tag: no draft, no artifacts,
  no release. Documented here so the move is never a mystery.
- The guard failure was diagnosed without Actions-log access from the
  sandbox (the blob host is network-blocked, as in B5); the signed job-log
  URL was fetched through an external page fetcher instead. Same
  substitution strategy, new mechanism.

## Known gaps

- **The draft is unpublished — owner decision:** publish unsigned now
  (SmartScreen warns until the certificate exists) or wait for the
  production certificate (Step 4) and cut a signed release. The banner and
  checksums make the unsigned state honest either way.
- F-03 (no production signing certificate) remains the only open Medium in
  the security review; closes automatically when the two secrets are added.
- Branch protection still unconfigured (admin action; token gets 403,
  re-verified today).
- Auto-update client deliberately absent (needs a published feed + stable
  signatures); `latest.yml` is not emitted (no publish provider) — the
  `.blockmap` update metadata was attached, `latest.yml` was not.
- G-05 (npm publication / `npx windows-runner`) still open.
- Installer fresh-clone + interactive modes remain CI-untested (pre-existing
  row, unchanged).
- No downgrade path tested for the installer (pre-existing row, unchanged).

## CI status

| Run | Commit / tag | Result | What |
| --- | --- | --- | --- |
| 35744980421 | `82154fe` | 9/9 GREEN | PR #33 (B5 merge record) |
| 35745916114 | `ae8fac2` | 9/9 GREEN | post-merge main, PR #33 |
| 35746819899 | tag `v0.1.0` @ `ae8fac2` | **FAILED** | first release attempt — guard `npm test` without build (see above); no draft created |
| 35749812077 | `b2811da` | 9/9 GREEN | PR #34 (guard fix) |
| 35750662456 | `e06c191` | 9/9 GREEN | post-merge main, PR #34 |
| 35751619807 | tag `v0.1.0` @ `e06c191` | 4/4 GREEN | release workflow: guard → CLI tarball → installer (install/e2e/uninstall) → draft created |

## Verdict

**GO — `v0.1.0` is cut and its DRAFT release is verified, pending human
review and the publish decision.** Nothing in this session is a STOP. The
next session's inputs: the maintainer's publish decision, the two owner
actions (certificate secrets, branch protection), and the Step 6 candidate
list (auto-update after a published feed + signatures; P1-01/P1-02/P1-03
residual; P1-04; G-05; installer modes).
