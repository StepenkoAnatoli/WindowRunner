/**
 * Types for scripts/checksums.mjs (see check-release.d.mts for the pattern).
 */

export declare const SUMS_FILE: string;
export declare function formatSums(files: string[]): string;
export declare function main(argv?: string[]): number;
